# set up

```
python3 -m venv speech
git clone git@github.com:mzgubic/speech.git
cd speech
source bin/activate
pip3 install pyaudio --global-option="build_ext" --global-option="-I/usr/local/include" --global-option="-L/usr/local/lib"
```

# usage
